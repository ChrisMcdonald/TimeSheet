
<%# if can? :read, User %>
  <%#= form_tag '', method: :get,  class: 'form-group row' do %>
<!--  <div class="col-lg-4">-->
    <%#= select_tag(:user_id, options_for_select(User.select_attributes), class: ' btn btn-default form-control') %>
<!--  </div>-->
    <%#= submit_tag 'Search', class: 'btn btn-default' %>
  <%# end %>
<%# end %>
<%= form_tag '',method: :get ,id: "project_form" do  %>
  <% project = params[:project_id].present? ? params[:project_id] : @user&.time_sheets&.last&.project_id %>
  <%= select_tag :project_id, options_from_collection_for_select(@user.projects, :id, :name, project), class: 'btn btn-default form-control', onchange: "$('#project_form').submit()" %>
<% end %>
  <%= my_calendar do |date| %>
    <%= date.strftime("%d  %b") %>
  <%end %>
