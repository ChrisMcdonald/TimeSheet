<li>
  <h3><%= chatroom.title %></h3><%= link_to "Join", chatroom_chatroom_users_path(chatroom), method: :post, class: 'btn btn-default' %>
  <%= link_to "Delete", chatroom_path(chatroom), method: :delete, class: 'btn btn-default', data: {confirm: "Are you sure?"} %>
  <%= link_to "Leave Chat", chatroom_chatroom_users_path(chatroom), method: :delete, class: 'btn btn-default', data: {confirm: "Are you sure?"} %>
</li>